{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "node .bin/npm-start.js",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "bootstrap": "lerna run bootstrap --hoist"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "@types/node": "^12.12.14",
    "lerna": "^3.19.0",
    "npm-execspawn": "^1.3.0",
    "prompts": "^2.3.0"
  }
}
